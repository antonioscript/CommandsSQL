--Sintaxe:
--MIN
SELECT 
    MIN(Price)
FROM 
    Products;

--1. 
SELECT 
    MIN(salary)
FROM
    employees;

--MAX
SELECT 
    MAX(Price)
FROM 
    Products;

--1. 
SELECT 
    MAX(salary)
FROM
    employees;